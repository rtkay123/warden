syntax = "proto3";

package configuration;

enum ConfigKind {
  ROUTING = 0;
  RULE = 1;
  TYPOLOGY = 2;
}

message ReloadEvent {
  ConfigKind kind = 1;
  optional string id = 2;
  optional string version = 3;
}
